-- ATC Housing Plugin — Shared Configuration

ATC = ATC or {}
ATC.Housing = ATC.Housing or {}

ATC.Housing.Config = {
    -- Maximum properties a single account may own simultaneously.
    MaxPropertiesPerAccount = 3,

    -- Property ID max length (UUID v4/v7 = 36 chars).
    PropertyIdMaxLength = 36,
}

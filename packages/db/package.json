{
  "name": "@atc/db",
  "version": "0.1.0",
  "private": true,
  "description": "ATC database client, migrations, and repositories",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "db:migrate": "node --experimental-vm-modules dist/migrate.js",
    "db:migrate:status": "node --experimental-vm-modules dist/migrate.js --status"
  },
  "dependencies": {
    "@atc/shared-types": "workspace:*",
    "mysql2": "^3.9.0",
    "ulidx": "^2.3.0"
  },
  "devDependencies": {
    "@atc/tsconfig": "workspace:*",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}

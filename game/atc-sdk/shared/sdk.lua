-- game/atc-sdk/shared/sdk.lua
-- ATC SDK version constants — available on both server and client.
-- External plugins can check ATC_SDK.Version to gate behaviour on SDK version.

ATC_SDK = ATC_SDK or {}

ATC_SDK.Version       = '0.1.0'
ATC_SDK.CompatVersion = '1'

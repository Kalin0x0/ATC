# ─────────────────────────────────────────────────────────────────────────────
# ATC — Atlantic Core  |  Infrastructure Environment Variables
# Copy this file to infra/.env and fill in real values before deploying.
# NEVER commit infra/.env to version control.
# ─────────────────────────────────────────────────────────────────────────────

# ── Database ──────────────────────────────────────────────────────────────────
DB_ROOT_PASSWORD=changeme_root
DB_PASSWORD=changeme_db

# Name and user of the ATC database. Both default to "atc" if left unset, and
# docker-compose applies them to the MariaDB container and the API alike.
# Only change them before the FIRST start — the db_data volume is created once.
DB_NAME=atc
DB_USER=atc

# ── Redis ─────────────────────────────────────────────────────────────────────
REDIS_PASSWORD=changeme_redis

# ── ATC API ───────────────────────────────────────────────────────────────────
# Read by the API. Must be at least 32 characters.
# The game server sends this as its Bearer token, so server.cfg's
# atc_api_token must be set to exactly the same value.
ATC_API_TOKEN=changeme_api_token_min32chars

# ── Game server values (NOT read by the API) ──────────────────────────────────
# These are kept here only so you have a single place to copy them from; the
# API never reads them and docker-compose does not pass them to the container.
# Mirror them into your server.cfg:
#     set atc_server_token "<ATC_SERVER_TOKEN>"
#     set atc_server_id    "<ATC_SERVER_ID>"
ATC_SERVER_TOKEN=changeme_server_token
ATC_SERVER_ID=atc-main-01

# ── Server ────────────────────────────────────────────────────────────────────
PORT=3000
